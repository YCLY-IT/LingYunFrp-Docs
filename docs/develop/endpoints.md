---
title: 接口列表
aside: false
---

# 接口列表

这一页把 `openapi.json` 里登记的全部接口摊在一页上，按分组排下来，适合整页搜索和通读。

左侧目录是同一份规范的树形视图：按分组收拢，每个接口一条，点进去是它自己的页面。往规范文件里加接口，这一页和左侧目录会一起变，不用改文档。

<ApiSpec />

::: tip 想调试接口
每个接口展开后都能看到完整的参数表、请求体字段和响应示例，页面底部还会按你的参数生成 cURL / JavaScript 请求示例，复制就能跑。
:::
