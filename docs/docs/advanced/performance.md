# 性能调优

加密、压缩、带宽限制等影响性能的配置项。

- `transport.useEncryption`：开启传输加密
- `transport.useCompression`：开启数据压缩
- `transport.bandwidthLimit`：限制带宽，单位 MB/s（与面板 Mbps 的换算见[配置文件说明](../parameters/config-file#带宽单位换算)）

> 本页待补充。